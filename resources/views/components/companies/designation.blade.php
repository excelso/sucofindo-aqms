<div class="form-group-control bg-white">
    <select class="form-control {{ $class }}" name="{{ $name }}" id="{{ $id }}" {{ $disabled }} data-selected="{{ $selected }}">
        <option value="">...</option>
    </select>
</div>
